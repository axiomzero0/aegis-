fn f(x: i32, a: i32, b: i32, c: i32, d: i32, e: i32, g: i32, h: i32, i: i32) -> i32 {
    if x > 0 {
        return (a + b) + (c + d) + (e + g) + (h + i);
    }
    return 0;
}
fn main() -> i32 {
    return f(1, 1, 2, 3, 4, 5, 6, 7, 8);
}
