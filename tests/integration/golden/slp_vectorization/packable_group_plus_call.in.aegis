fn g(v: i32) -> i32 {
    return v + 1;
}
fn f(x: i32, a: i32, b: i32, c: i32, d: i32, e: i32, h: i32, i: i32, j: i32) -> i32 {
    let p = g(x);
    return (a + b) + (c + d) + (e + h) + (i + j) + p;
}
fn main() -> i32 {
    return f(1, 2, 3, 4, 5, 6, 7, 8, 9);
}
