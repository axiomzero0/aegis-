// tools/repl/main.cpp — Interactive REPL (heavily utilizes the JIT).
// ============================================================
// Reads Aegis source line-by-line, lowers to IR, JIT-compiles on the
// fly, executes, and prints the result. Leverages the JitEngine to
// recompile hot lines on the next invocation.
// ============================================================
#include <iostream>
int main() {
    std::cout << "aegis-repl: not yet implemented\n";
    return 0;
}
