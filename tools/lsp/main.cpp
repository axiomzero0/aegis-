// tools/lsp/main.cpp — Language Server Protocol entry point.
// ============================================================
// Implements a JSON-RPC server on stdio that exposes diagnostics,
// goto-definition, and completion. Built on top of the compiler's
// frontend (Lexer, Parser, TypeChecker).
// ============================================================
#include <iostream>
int main() {
    std::cout << "aegis-lsp: not yet implemented\n";
    return 0;
}
